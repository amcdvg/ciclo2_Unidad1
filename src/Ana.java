package co.edu.utp.misiontic2022;
import java.lang.Math;
import java.util.Scanner; 

/*public class inversion {  //evaluar rentabilidad

    //Paso a paso -> Algoritmo    
    // 1) Recoger la información: int pTiempo, double pMonto, double pInteres 
    // 2) compararInversion
    // 3) Calcular el interesCompuesto
    // 4) Calcular el interesSimple
    // 5) Reportar la diferencia con el formato indicado, redondeado a dos cifras
    
    //Funciones que resuelven el problema -> Modularizado
    
    // 2) Comparar inversión
    //alcance tipo_de_retorno nombreFuncion(especificación argumentos){Bloque de código}

   public String compararInversion( int pTiempo, double pMonto, double pInteres ) {

        double respuesta = calcularInteresCompuesto(pTiempo, pMonto, pInteres) - calcularInteresSimple(pTiempo, pMonto, pInteres);
       
        respuesta = Math.round(respuesta * 100.0) / 100.0;
        return ("La diferencia en el total de intereses generados para el proyecto, si escogemos entre"  
        + " evaluarlo a una tasa de interés Compuesto y evaluarlo a una tasa de interés Simple, asciende "
        + "a la cifra de: $ " + String.format("%.2f", respuesta) + ".");


    }
     // 3) Calcular el interes compuesto
     public static double calcularInteresCompuesto(int pTiempo, double pMonto, double pInteres){
        
        double interesCompuesto = pMonto * (Math.pow (1+ pInteres,pTiempo)-1);
          
        return interesCompuesto;
          
     }
     // 4) Calcular el interes simple
     
     public static double calcularInteresSimple(int pTiempo, double pMonto, double pInteres){

            double interesSimple =  pTiempo * pMonto * pInteres;
        
            return interesSimple;
    }

    
    // 5) Reportar la diferencia con el formato indicado
    public static void reportarCompararInversion(String proyecto, double respuesta){
        System.out.println("La diferencia en el total de intereses generados para el proyecto, si escogemos entre" 
        + "evaluarlo a una tasa de interés Compuesto y evaluarlo a una tasa de interés Simple, asciende"
        + "a la cifra de: $ " + respuesta + ".");
    }*/


    